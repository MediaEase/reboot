{% extends 'pages/base.html.twig' %}

{% block title %}Group{% endblock %}

{% block body %}
    <h1>Group</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ group.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_settings_access_groups_index') }}">back to list</a>

    <a href="{{ path('app_group_edit', {'id': group.id}) }}">edit</a>

    {{ include('settings/access_groups/_delete_form.html.twig') }}
{% endblock %}
